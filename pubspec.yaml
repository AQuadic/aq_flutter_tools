name: aq_tools
description: Set of tools we use here at AQuadic Softwars.
version: 0.0.2
homepage: "https://AQuadic.com"

environment:
  sdk: ">=2.15.0 <3.0.0"
  flutter: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  quiver: ^3.0.1+1

flutter:
